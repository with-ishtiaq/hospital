# Production environment example (do NOT commit real secrets)
FRONTEND_URL="https://your-frontend.example.com"
JWT_SECRET="<replace-with-rotated-secret>"
DATABASE_URL="postgresql://<user>:<password>@<host>:<port>/<db>"
NODE_ENV="production"
SESSION_SECRET="<replace-with-rotated-secret>"
VERCEL_OIDC_TOKEN="<set in deployment provider; do NOT commit>"
